# =============================================================================
# Salin file ini menjadi ".env.local" lalu sesuaikan bila perlu.
# JANGAN commit file ".env.local" ke repository publik.
# =============================================================================

# Origin backend PHP (REST API + upload gambar). Tanpa trailing slash.
NEXT_PUBLIC_API_URL=http://localhost/backend

# Origin website publik — digunakan untuk tautan balik dan pratinjau konten.
NEXT_PUBLIC_MAIN_WEB_URL=http://localhost:3000
